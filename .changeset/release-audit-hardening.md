---
"a11y-menu-button": minor
---

Add an explicit core-only package entry and dedicated runtime add-on subpaths
while preserving all existing root exports.

Restore author-owned filter, async-state, feedback, and hint DOM state during
add-on teardown, and document the package's browser-support and privacy
boundaries.
