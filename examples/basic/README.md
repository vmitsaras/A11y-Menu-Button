# Basic example

Run `npm run build:dist`, serve the repository root with a local static server,
and open `/examples/basic/`. The example imports the core-only JavaScript entry
and the default package stylesheet from `dist`.
